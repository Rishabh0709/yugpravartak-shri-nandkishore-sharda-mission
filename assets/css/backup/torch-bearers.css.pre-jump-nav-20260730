:root {
    --profile-ink: #241b2e;
    --profile-parchment: #f7f1e6;
    --profile-parchment-deep: #eee3d0;
    --profile-flame: #b94f2b;
    --profile-gold: #987020;
    --profile-teal: #1e3a34;
    --profile-muted: #554b42;
    --profile-line: rgba(36, 27, 46, 0.12);
}

html {
    scroll-padding-top: 100px;
}

body.profile-page {
    background: var(--profile-parchment);
    color: var(--profile-ink);
    font-family: "Karla", sans-serif;
    font-size: 18px;
    line-height: 1.7;
}

.profile-page .site-header,
.profile-page .site-nav,
.profile-page .footer {
    font-family: "Karla", sans-serif;
}

.profile-page .site-title,
.profile-page .footer-title {
    font-family: "Fraunces", serif;
}

.profile-page .site-nav a,
.profile-page .footer-navigation a {
    font-size: 16.5px;
}

.profile-page .copyright {
    font-size: 14px;
}

.profile-skip-link {
    position: fixed;
    z-index: 2000;
    top: 12px;
    left: 12px;
    padding: 10px 16px;
    border-radius: 999px;
    background: var(--profile-ink);
    color: #fffaf2;
    font-weight: 600;
    transform: translateY(-160%);
    transition: transform 180ms ease;
}

.profile-skip-link:focus {
    transform: translateY(0);
}

.profile-wrap {
    width: min(calc(100% - 64px), 1120px);
    margin-inline: auto;
}

.profile-eyebrow {
    display: block;
    color: var(--profile-flame);
    font-family: "Cinzel", serif;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
}

.profile-hero {
    position: relative;
    padding: 82px 0 92px;
    overflow: hidden;
    background:
        radial-gradient(circle at 82% 22%, rgba(152, 112, 32, 0.14), transparent 31%),
        radial-gradient(circle at 13% 92%, rgba(185, 79, 43, 0.09), transparent 30%),
        linear-gradient(180deg, #faf6ee 0%, var(--profile-parchment-deep) 100%);
}

.profile-hero::after {
    position: absolute;
    right: -130px;
    bottom: -190px;
    width: 420px;
    height: 420px;
    content: "";
    border: 1px solid rgba(152, 112, 32, 0.16);
    border-radius: 50%;
}

.profile-breadcrumb {
    display: inline-flex;
    margin-bottom: 34px;
    align-items: center;
    gap: 9px;
    color: var(--profile-muted);
    font-size: 15px;
}

.profile-breadcrumb a {
    border-bottom: 1px solid rgba(85, 75, 66, 0.3);
}

.profile-breadcrumb a:hover {
    color: var(--profile-flame);
    border-color: var(--profile-flame);
}

.profile-hero-grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(320px, 0.72fr);
    gap: clamp(48px, 7vw, 90px);
    align-items: center;
}

.profile-title {
    max-width: 680px;
    margin-top: 13px;
    font-family: "Fraunces", serif;
    font-size: clamp(44px, 6vw, 70px);
    font-weight: 400;
    letter-spacing: -0.025em;
    line-height: 1.04;
}

.profile-role {
    margin-top: 17px;
    color: var(--profile-gold);
    font-family: "Fraunces", serif;
    font-size: clamp(21px, 2.6vw, 28px);
    font-style: italic;
    line-height: 1.4;
}

.profile-lead {
    max-width: 670px;
    margin-top: 25px;
    color: var(--profile-muted);
    font-size: 19px;
    line-height: 1.75;
}

.profile-portrait-shell {
    position: relative;
    max-width: 390px;
    margin-left: auto;
    padding: 12px;
    border: 1px solid rgba(152, 112, 32, 0.3);
    border-radius: 190px 190px 28px 28px;
    background: rgba(255, 255, 255, 0.45);
    box-shadow: 0 28px 70px rgba(36, 27, 46, 0.12);
}

.profile-portrait-shell::before {
    position: absolute;
    z-index: -1;
    inset: -24px;
    content: "";
    border: 1px solid rgba(185, 79, 43, 0.09);
    border-radius: 210px 210px 42px 42px;
}

.profile-portrait {
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    object-position: center 18%;
    border-radius: 178px 178px 20px 20px;
}

.profile-portrait--bhaiyaji {
    object-position: center 20%;
}

.profile-facts {
    position: relative;
    z-index: 3;
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin-top: -38px;
    overflow: hidden;
    border: 1px solid rgba(152, 112, 32, 0.18);
    border-radius: 18px;
    background: rgba(255, 253, 249, 0.94);
    box-shadow: 0 18px 46px rgba(36, 27, 46, 0.08);
}

.profile-fact {
    min-height: 126px;
    padding: 25px 30px;
}

.profile-fact + .profile-fact {
    border-left: 1px solid rgba(152, 112, 32, 0.17);
}

.profile-fact span {
    display: block;
    margin-bottom: 7px;
    color: var(--profile-flame);
    font-family: "Cinzel", serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
}

.profile-fact strong {
    display: block;
    font-family: "Fraunces", serif;
    font-size: 22px;
    font-weight: 500;
    line-height: 1.3;
}

.profile-story {
    padding: 100px 0 84px;
}

.profile-story-intro {
    max-width: 780px;
    margin-bottom: 64px;
}

.profile-story-intro h2 {
    margin-top: 11px;
    font-family: "Fraunces", serif;
    font-size: clamp(34px, 4.6vw, 50px);
    font-weight: 400;
    line-height: 1.14;
}

.profile-story-intro p {
    margin-top: 20px;
    color: var(--profile-muted);
    font-size: 19px;
    line-height: 1.75;
}

.profile-emotion-opening {
    display: grid;
    grid-template-columns: minmax(280px, 0.82fr) minmax(0, 1.18fr);
    margin: 10px 0 72px;
    overflow: hidden;
    border: 1px solid rgba(152, 112, 32, 0.18);
    border-radius: 26px;
    background: rgba(255, 252, 246, 0.78);
    box-shadow: 0 24px 60px rgba(36, 27, 46, 0.07);
}

.profile-emotion-opening figure,
.profile-archival-frame,
.profile-wide-memory {
    margin: 0;
}

.profile-emotion-opening img {
    width: 100%;
    height: 100%;
    min-height: 510px;
    object-fit: cover;
}

.profile-emotion-opening .portrait-top {
    object-position: center 18%;
}

.profile-emotion-opening .opening-memory {
    object-position: 68% center;
}

.profile-emotion-copy {
    display: flex;
    padding: clamp(38px, 6vw, 76px);
    flex-direction: column;
    justify-content: center;
}

.profile-scene-kicker {
    color: var(--profile-flame);
    font-family: "Cinzel", serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
}

.profile-emotion-copy h2,
.profile-narrative-copy h2 {
    margin: 12px 0 20px;
    font-family: "Fraunces", serif;
    font-size: clamp(31px, 4vw, 45px);
    font-weight: 400;
    line-height: 1.15;
}

.profile-emotion-copy p,
.profile-narrative-copy p {
    color: var(--profile-muted);
    font-size: 18px;
    line-height: 1.78;
    text-align: justify;
    hyphens: auto;
}

.profile-emotion-copy p + p,
.profile-narrative-copy p + p {
    margin-top: 18px;
}

.profile-inner-voice {
    max-width: 920px;
    margin: 0 auto 36px;
    padding: 58px clamp(24px, 6vw, 72px);
    color: var(--profile-ink);
    border-top: 1px solid rgba(152, 112, 32, 0.3);
    border-bottom: 1px solid rgba(152, 112, 32, 0.3);
    font-family: "Fraunces", serif;
    font-size: clamp(27px, 3.6vw, 40px);
    font-style: italic;
    font-weight: 300;
    line-height: 1.42;
    text-align: center;
}

.profile-inner-voice span {
    display: block;
    margin-bottom: 16px;
    color: var(--profile-gold);
    font-family: "Cinzel", serif;
    font-size: 11px;
    font-style: normal;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
}

.profile-narrative-pair {
    display: grid;
    grid-template-columns: minmax(0, 1.03fr) minmax(290px, 0.97fr);
    gap: clamp(38px, 7vw, 88px);
    padding: 72px 0;
    align-items: center;
    border-bottom: 1px solid var(--profile-line);
}

.profile-narrative-pair.is-reversed {
    grid-template-columns: minmax(290px, 0.97fr) minmax(0, 1.03fr);
}

.profile-narrative-pair.is-reversed .profile-narrative-copy {
    order: 2;
}

.profile-archival-frame {
    position: relative;
    padding: 12px;
    border: 1px solid rgba(152, 112, 32, 0.2);
    border-radius: 20px;
    background: rgba(255, 252, 246, 0.78);
    box-shadow: 0 18px 44px rgba(36, 27, 46, 0.08);
}

.profile-archival-frame img,
.profile-wide-memory img {
    display: block;
    width: 100%;
    border-radius: 12px;
    object-fit: cover;
}

.profile-archival-frame img {
    max-height: 520px;
}

.profile-archival-frame figcaption,
.profile-wide-memory figcaption {
    padding: 13px 5px 2px;
    color: var(--profile-muted);
    font-size: 14px;
    line-height: 1.5;
}

.profile-turning-point {
    position: relative;
    margin: 26px 0 18px;
    padding: clamp(42px, 7vw, 76px);
    overflow: hidden;
    color: #fffaf2;
    border-radius: 24px;
    background:
        radial-gradient(circle at 88% 14%, rgba(211, 173, 92, 0.2), transparent 28%),
        linear-gradient(145deg, #2f554b, #223f39);
}

.profile-turning-point::after {
    position: absolute;
    right: -55px;
    bottom: -90px;
    width: 250px;
    height: 250px;
    content: "";
    border: 1px solid rgba(211, 173, 92, 0.2);
    border-radius: 50%;
}

.profile-turning-point .profile-scene-kicker {
    color: #d3ad5c;
}

.profile-turning-point h2 {
    max-width: 760px;
    margin: 12px 0 20px;
    font-family: "Fraunces", serif;
    font-size: clamp(32px, 4.6vw, 50px);
    font-weight: 400;
    line-height: 1.14;
}

.profile-turning-point p {
    max-width: 790px;
    color: #e8e0d8;
    font-size: 18px;
    line-height: 1.78;
    text-align: justify;
    hyphens: auto;
}

.profile-turning-point p + p {
    margin-top: 17px;
}

.profile-wide-memory {
    margin: 72px 0 18px;
}

.profile-wide-memory img {
    max-height: 610px;
}

.profile-wide-memory figcaption {
    padding-top: 15px;
    text-align: center;
}

.profile-chapter {
    display: grid;
    grid-template-columns: minmax(190px, 0.42fr) minmax(0, 1fr);
    gap: clamp(38px, 6vw, 82px);
    padding: 54px 0;
    border-top: 1px solid var(--profile-line);
}

.profile-chapter-number {
    display: block;
    margin-bottom: 13px;
    color: var(--profile-gold);
    font-family: "Fraunces", serif;
    font-size: 18px;
    font-style: italic;
}

.profile-chapter h2 {
    max-width: 300px;
    font-family: "Fraunces", serif;
    font-size: clamp(29px, 3.3vw, 39px);
    font-weight: 400;
    line-height: 1.16;
}

.profile-chapter-copy {
    max-width: 700px;
}

.profile-chapter-copy p {
    color: var(--profile-muted);
    font-size: 18px;
    line-height: 1.78;
}

.profile-chapter-copy p + p {
    margin-top: 18px;
}

.profile-reflection {
    position: relative;
    max-width: 850px;
    margin: 26px auto 22px;
    padding: 42px 48px;
    color: var(--profile-ink);
    border-top: 1px solid rgba(152, 112, 32, 0.3);
    border-bottom: 1px solid rgba(152, 112, 32, 0.3);
    font-family: "Fraunces", serif;
    font-size: clamp(23px, 3vw, 31px);
    font-style: italic;
    font-weight: 300;
    line-height: 1.5;
    text-align: center;
}

.profile-reflection::before {
    position: absolute;
    top: 0;
    left: 50%;
    padding: 0 14px;
    content: "✦";
    color: var(--profile-gold);
    background: var(--profile-parchment);
    font-size: 15px;
    font-style: normal;
    transform: translate(-50%, -50%);
}

.profile-contribution {
    padding: 84px 0;
    color: #f2eadf;
    background:
        radial-gradient(circle at 85% 0%, rgba(152, 112, 32, 0.2), transparent 34%),
        linear-gradient(180deg, var(--profile-teal), #274a42);
}

.profile-contribution-header {
    max-width: 700px;
}

.profile-contribution .profile-eyebrow {
    color: #d3ad5c;
}

.profile-contribution h2 {
    margin-top: 11px;
    font-family: "Fraunces", serif;
    font-size: clamp(34px, 4.5vw, 48px);
    font-weight: 400;
    line-height: 1.15;
}

.profile-principles {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 20px;
    margin-top: 46px;
}

.profile-principle {
    padding: 29px 28px;
    border: 1px solid rgba(242, 234, 223, 0.18);
    background: rgba(255, 255, 255, 0.035);
}

.profile-principle span {
    color: #d3ad5c;
    font-family: "Cinzel", serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
}

.profile-principle h3 {
    margin: 9px 0 9px;
    font-family: "Fraunces", serif;
    font-size: 25px;
    font-weight: 400;
}

.profile-principle p {
    color: #ded6e4;
    font-size: 17px;
    line-height: 1.7;
}

.profile-lineage {
    padding: 86px 0 94px;
}

.profile-lineage-header {
    max-width: 720px;
    margin: 0 auto 42px;
    text-align: center;
}

.profile-lineage-header h2 {
    margin-top: 10px;
    font-family: "Fraunces", serif;
    font-size: clamp(32px, 4vw, 44px);
    font-weight: 400;
}

.profile-lineage-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
}

.profile-lineage-card {
    display: flex;
    min-height: 150px;
    padding: 25px 26px;
    flex-direction: column;
    justify-content: space-between;
    border: 1px solid rgba(36, 27, 46, 0.1);
    border-top-color: rgba(152, 112, 32, 0.34);
    border-radius: 16px;
    background: rgba(255, 255, 255, 0.48);
    box-shadow: 0 14px 36px rgba(36, 27, 46, 0.055);
    transition: transform 220ms ease, box-shadow 220ms ease, border-color 220ms ease;
}

a.profile-lineage-card:hover {
    border-color: rgba(152, 112, 32, 0.34);
    box-shadow: 0 20px 42px rgba(36, 27, 46, 0.09);
    transform: translateY(-3px);
}

.profile-lineage-card.is-current {
    background: rgba(152, 112, 32, 0.08);
    box-shadow: none;
}

.profile-lineage-role {
    color: var(--profile-flame);
    font-family: "Cinzel", serif;
    font-size: 11.5px;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
}

.profile-lineage-card strong {
    display: block;
    margin-top: 8px;
    font-family: "Fraunces", serif;
    font-size: 24px;
    font-weight: 500;
    line-height: 1.25;
}

.profile-lineage-action {
    margin-top: 17px;
    color: var(--profile-gold);
    font-size: 15px;
    font-weight: 600;
}

.profile-closing {
    display: flex;
    margin-top: 46px;
    padding-top: 34px;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 14px;
    border-top: 1px solid var(--profile-line);
}

.profile-button {
    display: inline-flex;
    min-height: 50px;
    padding: 13px 24px;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--profile-ink);
    border-radius: 999px;
    font-family: "Cinzel", serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.11em;
    text-transform: uppercase;
    transition: color 200ms ease, background 200ms ease, transform 200ms ease;
}

.profile-button--primary {
    color: #fffaf2;
    border-color: var(--profile-flame);
    background: var(--profile-flame);
}

.profile-button:hover {
    color: #fffaf2;
    background: var(--profile-ink);
    transform: translateY(-2px);
}

@media (max-width: 820px) {
    .profile-wrap {
        width: min(calc(100% - 40px), 720px);
    }

    .profile-hero {
        padding: 62px 0 78px;
    }

    .profile-hero-grid {
        grid-template-columns: 1fr;
    }

    .profile-portrait-shell {
        width: min(100%, 390px);
        margin: 0 auto;
    }

    .profile-facts,
    .profile-principles,
    .profile-lineage-grid {
        grid-template-columns: 1fr;
    }

    .profile-fact + .profile-fact {
        border-top: 1px solid rgba(152, 112, 32, 0.17);
        border-left: 0;
    }

    .profile-fact {
        min-height: 0;
    }

    .profile-chapter {
        grid-template-columns: 1fr;
        gap: 24px;
    }

    .profile-chapter h2 {
        max-width: 620px;
    }

    .profile-emotion-opening,
    .profile-narrative-pair,
    .profile-narrative-pair.is-reversed {
        grid-template-columns: 1fr;
    }

    .profile-emotion-opening img {
        min-height: 0;
        max-height: 590px;
    }

    .profile-narrative-pair.is-reversed .profile-narrative-copy {
        order: 0;
    }

    .profile-archival-frame {
        width: min(100%, 620px);
        margin: 0 auto;
    }
}

@media (max-width: 520px) {
    .profile-wrap {
        width: min(calc(100% - 32px), 720px);
    }

    .profile-hero {
        padding: 50px 0 68px;
    }

    .profile-title {
        font-size: clamp(39px, 12vw, 54px);
    }

    .profile-lead,
    .profile-story-intro p,
    .profile-chapter-copy p,
    .profile-emotion-copy p,
    .profile-narrative-copy p,
    .profile-turning-point p {
        font-size: 17px;
        text-align: left;
    }

    .profile-facts {
        margin-top: -24px;
        border-radius: 14px;
    }

    .profile-story {
        padding: 78px 0 62px;
    }

    .profile-story-intro {
        margin-bottom: 42px;
    }

    .profile-emotion-opening {
        margin-bottom: 52px;
        border-radius: 18px;
    }

    .profile-emotion-copy {
        padding: 34px 24px 40px;
    }

    .profile-inner-voice {
        padding: 44px 16px;
    }

    .profile-narrative-pair {
        gap: 28px;
        padding: 52px 0;
    }

    .profile-turning-point {
        padding: 38px 24px;
        border-radius: 18px;
    }

    .profile-wide-memory {
        margin-top: 54px;
    }

    .profile-chapter {
        padding: 42px 0;
    }

    .profile-reflection {
        padding: 34px 20px;
    }

    .profile-contribution,
    .profile-lineage {
        padding: 66px 0;
    }
}

@media (prefers-reduced-motion: reduce) {
    .profile-lineage-card,
    .profile-button,
    .profile-skip-link {
        transition: none;
    }
}
